/target
**/*.rs.bk
.env
*.db
.gstack/

# Python (do.py task runner) bytecode.
__pycache__/
*.pyc

# Deploy: ssh keys, per-env secret env files, build artifacts.
# The committed deploy/*.env.example template stays tracked.
deploy/keys/
deploy/dist/
deploy/*.env
!deploy/*.env.example

# Runtime data (sqlite/logs written under a data/ sibling of configuration/).
/data/

# QA config carries the real Postgres URL/password — pushed at deploy time.
deploy/qa-ccg.json

# Market / GTM / MRR research — keep local only, never publish
RESEARCH-FINDINGS.md

# Brainstorming visual-companion scratch dir
.superpowers/
